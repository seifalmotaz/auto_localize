/// Auto Localize
///
/// A command-line utility that automatically scans your Flutter project to find
/// hardcoded UI strings and replaces them with localization keys using GetX's
/// `.tr` and `.trParams` methods.
library auto_localize;

export 'src/localize.dart';
